kind = "web"
previewPath = "/"
title = "WhatsApp Monitor"
version = "1.0.0"
id = "artifacts/whatsapp-monitor"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19562

[services.development]
run = "pnpm --filter @workspace/whatsapp-monitor run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/whatsapp-monitor", "run", "build" ]
publicDir = "artifacts/whatsapp-monitor/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19562"
BASE_PATH = "/"
